---
Mapping\Fixture\Yaml\BaseCategory:
  type: mappedSuperclass
  fields:
    lft:
      type: integer
      gedmo:
        - treeLeft
    rgt:
      type: integer
      gedmo:
        - treeRight
    root:
      type: integer
      gedmo:
        - treeRoot
    lvl:
      type: integer
      gedmo:
        - treeLevel
    created:
      type: datetime
      gedmo:
        timestampable:
          on: create
    updated:
      type: date
      gedmo:
        timestampable:
          on: update
