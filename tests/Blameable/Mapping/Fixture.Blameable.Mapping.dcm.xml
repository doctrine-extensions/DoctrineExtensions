<?xml version="1.0" encoding="UTF-8"?>

<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:gedmo="http://gediminasm.org/schemas/orm/doctrine-extensions-mapping">

    <entity name="Fixture\Blameable\Mapping">

        <id name="id" type="integer" column="id">
            <generator strategy="AUTO"/>
        </id>

        <field name="title" type="string" length="32" />
        <field name="body" type="text" nullable="true" />
        <field name="published" type="boolean" />

        <field name="createdBy">
            <gedmo:blameable on="create" />
        </field>

        <field name="updatedBy">
            <gedmo:blameable on="update" />
        </field>

        <field name="changedBy" nullable="true">
            <gedmo:blameable on="change" field="title, body"/>
        </field>

        <field name="publishedBy" nullable="true">
            <gedmo:blameable on="change" field="published" value="true" />
        </field>

    </entity>

</doctrine-mapping>
